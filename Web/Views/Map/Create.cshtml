@model CoreLibrary.Models.EFModels.Map

@{
    ViewData["Title"] = "Создать карту";
    Layout = "Layout/_LayoutUserAuth";
    ViewData["Active_LocationTableData_Link"] = "active";
}

<div class="container-fluid">
    <h3 class="text-dark mb-4">Создать карту</h3>
    <div class="card shadow">
        <div class="card-body">
            <form asp-action="Create">
                <div class="mb-3">
                    <label asp-for="Name" class="form-label"></label>
                    <input asp-for="Name" class="form-control"/>
                </div>

                <div class="mb-3">
                    <label asp-for="Description" class="form-label"></label>
                    <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                </div>

                <button type="submit" class="btn btn-primary">Создать</button>
                <a asp-action="MapTableData" class="btn btn-secondary">Отмена</a>
            </form>
        </div>
    </div>
</div>